name: Greetings

on: [pull_request, issues]

jobs:
  greeting:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/first-interaction@v1
      with:
        repo-token: ${{ secrets.GITHUB_TOKEN }}
        issue-message: 'Hello, Thanks for contributing to StudentRant. We <3 you'' first issue'
        pr-message: 'Hello, Thanks for contributing to StudentRant. We <3 you'' first pr'
